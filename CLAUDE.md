# Claude Code guidance

Start with [AGENTS.md](AGENTS.md), then read `.github/AGENT_WORKFLOW.md`, the active AIQT packet and `suggestedFiles`, and only relevant `docs/`.

- Inspect before modifying; never claim to have read files you did not read.
- Follow the active AIQT WU; prefer `suggestedFiles`, expanding context only when evidence requires it.
- Implement when requested. Use subagents only for genuinely independent work.
- Never bypass validation, use `--no-verify`, force-push, discard unfamiliar work, or start the next WU.
- Stop WU-started processes before closure and return evidence to the Project Overseer.
