# Development workflow

The Project Overseer defines and approves work. AIQT records its canonical operational state: milestones, WUs, dependencies, packets, suggested files, checkpoints, evidence, and handoff. The Development Agent reads the active WU, implements only its bounded scope, validates it, records evidence, commits it, and stops for Overseer review.
